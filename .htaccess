<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /kit-services/

    # Redirige tout vers public/
    RewriteCond %{REQUEST_URI} !^public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
